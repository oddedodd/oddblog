# Sanity Studio — oddblog

Content studio for oddblog. Schemas live in `schemaTypes/`.

## Commands

```sh
npm run dev      # Start at http://localhost:3333
npm run deploy   # Deploy to sanity.io
```

## Schema

- **Post** — blog posts with title, slug, date, excerpt, and body
